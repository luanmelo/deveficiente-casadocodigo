server:
  port: 8080

spring:
  application:
    name: casadocodigo
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jmx:
    enable: false
  datasource:
    url: jdbc:mysql://localhost:3306/casadocodigo?createDatabaseIfNotExist=true
    username: root
    password: deveficiente

